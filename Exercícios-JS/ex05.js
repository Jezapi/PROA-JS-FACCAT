// Cálculo da área de um quadrado e seu dobro
const lado = parseFloat(prompt("Digite o comprimento do lado do quadrado: "));
const area = lado * lado;
const dobroArea = area * 2;
console.log("A área do quadrado é", area, "e o dobro da área é", dobroArea);
